NEWS
====

0.4.0 (2015-01-14)
------------------

* Support Python2.x/Python 3.x.

0.3.0 (2015-01-13)
------------------

* Support ``circle:`` in context.
* Support to disable zoom control.
* Support ``:offline:`` option for tiles.

0.2.0 (2015-01-12)
------------------

* Support ``zoom`` option.
* Add examples.
* Support to omit ``label:`` in context.
* Drop ``:label:`` option. This is incompatible change. Specify label as arguments.
* Drop ``latitude:``, ``longitude:``. Use ``location:`` in context instead.
* Support milliseconds expression in ``location:``.
* Support ``rectangle:`` in context.

0.1.0 (2015-01-11)
------------------

* Initial release
